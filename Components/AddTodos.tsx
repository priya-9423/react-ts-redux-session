import React, { PureComponent } from "react";

export default class AddTodos extends PureComponent {
  render() {
    return (
      <div>
        <input type="text" />
        <button onClick = >Add</button>
      </div>
    );
  }
}
